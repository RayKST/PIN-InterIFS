<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/index.css" type="text/css">
        <!-- <link rel="stylesheet" href="/styles.css" type="text/css"> -->
        <title>Página Inicial</title>
    </head>
<body>
  <%- include('partials/sidebar') %>
  <a href="criar-torneio" class="torneio_criar"><button><strong>Criar torneio</strong></button></a>
  <div class="content">
    <h1>Torneios disponíveis </h1>
    <% if(tornaments){ %>
      <% for (let i = 0; i < tornaments.length; i++) { %>
        <li><%= tornaments[i].dataValues.name%> - <%= tornaments[i].dataValues.description %></li>
        <button value=<%= tornaments[i].dataValues.id %>>Participar do torneio</button>
      <% } %>
    <% } %>
  </div>

  <div class="background-icon">

  </div>
</body>
</html>