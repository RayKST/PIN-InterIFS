<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/index.css" type="text/css">
    <link rel="stylesheet" href="/cards.css" type="text/css">
    <title>Lista de Times</title>
</head>

<body>
    <%- include('partials/sidebar') %>
    <a href="criar-time" class="time_criar"><button><strong>Criar time</strong></button></a>
    <h1>Times</h1>
    <div class="cards">
        <% if(teams){ %>
            <% for (let i = 0; i < teams.length; i++) { %>

                <form action="" method="post" class="card">
                    <h2><%= teams[i].dataValues.name%> </h2>
                    <p><b>Descrição:</b> <%= teams[i].dataValues.description %></p>
                    <p><b>Máximo de membros:</b> <%= teams[i].dataValues.numberOfMembers%></p>
                    <p><b>Número de membros atuais: </b> <%= teamsInfo[i].totalMembers %></p>
                    <p><b>Modalidade principal:</b> <%= teamsInfo[i].sportName %></p>
                    <p><b>Criador do time: </b> <%= teamsInfo[i].creatorName %></p>

                    <input style="visibility: hidden; display: none;" type="number" name="teamID" value=<%= teams[i].dataValues.id %>>
                    <button type="submit" value=<%= teams[i].dataValues.id %>>Ingressar ao time</button>
                </form>

            <% } %>
        <% } %>
    </ul>
</body>
</html>